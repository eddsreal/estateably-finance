# Plan Quality Checklist: Personal Finance Manager

**Purpose**: Requirements-quality review of the plan phase — `plan.md`, `research.md`, `data-model.md`, `quickstart.md` and the frozen contracts — before the `review-plan` gate and `/speckit-tasks`. Every item interrogates what the plan _says_ (completeness, clarity, consistency, traceability), not what the future implementation will do.
**Created**: 2026-09-21
**Feature**: [plan.md](../plan.md) · [spec.md](../spec.md) · **Governing document**: [constitution.md](../../../.specify/memory/constitution.md) v1.0.0

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Decision Completeness

- [x] CHK001 Is every decision the spec's Assumptions section delegated to the plan closed in a research entry, with none surviving only as an implicit choice in the source tree or contract? [Completeness, R-001–R-016, Spec §Assumptions]
- [x] CHK002 Is the mapping from database-constraint violations to the frozen error codes specified — a unique-index hit that races past the service check (→ `DUPLICATE_NAME`?), and the deferred sum-to-zero trigger raising at commit — given the exception filter maps only _typed domain errors_? [Gap, R-011, Data-model §Raw SQL, Const §V]
- [x] CHK003 Is behaviour specified for a failed `ledger:check` reconciliation against a live database — who runs it outside CI, and whether `ledger:rebuild` is invoked automatically or by hand? [Gap, Recovery, Plan §Constitution Check I, Spec §FR-031]
- [x] CHK004 Are the per-intent amount sign rules (positive for expense/income/transfer, signed non-zero for opening) fixed in the frozen OpenAPI schemas, or explicitly assigned to domain validation with the wire pattern documented as sign-agnostic? [Completeness, Plan §Constitution Check II, Data-model §Intent → entries, contracts/openapi.yaml]
- [x] CHK005 Is `NODE_ENV` documented where a deployer will find it, given R-006 derives production HSTS from it while FR-035 deliberately excludes it from the env-var list and `.env.example`? [Gap, R-006, Spec §FR-035]
- [x] CHK006 Are all six FR-035 environment variables given example values in the planned `.env.example`, including `DATABASE_URL`, which R-016 lists without a default? [Completeness, R-016, Spec §FR-035]
- [x] CHK007 Is the `/docs` build step specified concretely enough to build from — which tool copies `swagger-ui-dist` + the yaml into `public/docs`, when it runs, and how dev on port 5173 gets the same page? [Completeness, R-014, Plan §Project Structure]
- [x] CHK008 Does the Foundational phase's "CI green end to end" define what the Playwright stage runs while no story screen exists yet, given UI e2e specs are one-per-story? [Completeness, Plan §Story × module matrix, §Tests & CI]

## Clarity & Ambiguity

- [x] CHK009 Is the project-delete rule stated as a single unambiguous final rule, given data-model.md leaves the deliberation ("including soft-deleted ones? — no: …") inline in the normative text? [Clarity, Data-model §Project, Spec §FR-019]
- [x] CHK010 Is the number of overdue occurrences a recurring item contributes to the _projection_ defined in a plan artifact? Data-model fixes confirmation advance (one missed period per confirmation) but not projection counting. [Ambiguity, Data-model §State transitions, Spec §FR-017]
- [x] CHK011 Does the FR-028 freshness rule name every query family invalidated per mutation — the plan invalidates "balance queries", but transaction lists, reports, project spend and projection also read entry-derived data? [Clarity, Plan §Frontend implementation rules, Spec §FR-028]
- [x] CHK012 Is the seed's determinism claim reconciled with dates computed relative to the seed-run "today" — does FR-025's "deterministic" mean identical output per run date, and is that stated? [Ambiguity, Data-model §Seed, Spec §FR-025]
- [x] CHK013 Is BalanceSnapshot's coverage of archived accounts specified — whether archived accounts keep snapshot rows, and where the "total across non-archived accounts" filter is applied? [Clarity, Data-model §BalanceSnapshot, R-007, Spec §FR-010]
- [x] CHK014 Is the outcome defined for the should-be-impossible path where the domain sum-to-zero check passes but the deferred trigger still raises at commit — which error code and status leave the API? [Edge Case, Data-model §Raw SQL, R-011]

## Internal Consistency

- [x] CHK015 Does the Constitution Check's "two justified entries in Complexity Tracking" match the three rows the Complexity Tracking table actually contains (one unconditional, two conditional)? [Consistency, Plan §Constitution Check, §Complexity Tracking]
- [x] CHK016 Are the countable claims — 8 screens, 23 API paths / 31 operations, 7 modules — each verifiable against the frozen yaml, the `features/` list and the module graph, and currently in agreement? [Consistency, Measurability, Plan §Technical Context, contracts/openapi.yaml]
- [x] CHK017 Is the hard-coded openapi-fetch `baseUrl` constant reconciled with R-004's rationale that origins are deployment configuration (`CORS_ORIGINS` env var) — is the asymmetry deliberate and its ceiling recorded? [Consistency, R-004, Plan §Project Structure]
- [x] CHK018 Do the two module dependency graphs (plan.md and R-015) agree edge for edge, including `reports → categories` and the scheduled-items path to the ledger via accounts? [Consistency, Plan §Module dependency graph, R-015]
- [x] CHK019 Are the prototype behaviours deferred to feature 002 (Undo toast, N shortcut, row flash, modal dirty-guard) each confirmed absent from the spec's committed requirements — deferred from the prototype only, not from an FR? [Consistency, Plan §Frontend implementation rules, Spec §Out of Scope]

## Spec Traceability & Coverage

- [x] CHK020 Is every functional requirement FR-001–FR-035 traceable to at least one plan element (module, contract path, screen, script or CI step), with none silently dropped in the plan phase? [Traceability, Plan, Spec §Functional Requirements]
- [x] CHK021 Does the planned e2e suite name a trigger for every R-011 error code, including the three provider-failure codes (`AI_PROVIDER_ERROR`, `AI_RATE_LIMITED`, `AI_TIMEOUT`) that cannot be produced by the real provider in CI — is a stubbing approach specified? [Coverage, Gap, R-011, R-012, Plan §Tests & CI, Spec §SC-006]
- [x] CHK022 Are FR-034's accessibility-floor requirements carried into concrete plan rules for the custom `Controller`-wrapped pickers — keyboard operation, labels, focus order — in terms the keyboard-only Playwright specs can check? [Coverage, Plan §Frontend implementation rules, Spec §FR-034, §SC-012]
- [x] CHK023 Is SC-009's dollars-never-cents guarantee enforced by something mechanical on the web side (named component-test assertions via `money.ts`), or only by convention? [Coverage, Plan §Frontend implementation rules, Spec §SC-009]
- [x] CHK024 Is the pagination contract (default 50, max 200, `total` in every list response) stated to apply uniformly across all list endpoints in the frozen yaml, including report drill-downs? [Consistency, R-016, contracts/openapi.yaml, Spec §FR-009]

## Constitution Alignment

- [x] CHK025 Is a stated reason recorded for every runtime dependency, and is `swagger-ui-dist` classified (runtime asset vs dev tooling) with its reason, as the constitution's stack constraints require? [Traceability, R-001, Const §Stack]
- [x] CHK026 Does the plan enumerate which rules the database enforces (name uniqueness, entry shape, sum-to-zero, FKs) versus which are deliberately application-level (category-type match, archived checks, closed project), so "DB constraints preferred" is auditable rather than assumed? [Completeness, Data-model §Raw SQL, Const §Stack]
- [x] CHK027 Is Principle IV's freeze operationalized against edits to the contract files themselves? `contract:check` catches code-vs-yaml drift; is anything specified to surface a change to `contracts/openapi.yaml` or `schema.prisma` during implementation beyond ad-hoc review? [Gap, Const §IV, R-014]
- [x] CHK028 Are both mechanical boundary guards specified with exact rule configurations — pure `domain/` (no `@nestjs/*`, `@prisma/*`) and no cross-module internals (only `services/`) — and does the no-barrel-files rule they depend on appear as a normative convention? [Traceability, R-003, Plan §Structure Decision, Const §VI, §VII]

## Measurability & Acceptance

- [x] CHK029 Do R-013's measured endpoints and thresholds exactly cover SC-008's wording — same percentile, same dataset, same endpoint set — so the manual `perf:measure` run is the criterion's test and not a proxy? [Measurability, R-013, Spec §SC-008]
- [x] CHK030 Does quickstart.md's walkthrough script give SC-001/SC-012 a concrete step list that the demo seed's data actually supports (accounts, categories, scheduled items, project named in the steps)? [Measurability, quickstart.md, Data-model §Seed, Spec §SC-001]
- [x] CHK031 Are the two R-002 Setup verifications phrased as pass/fail checks with pre-approved fallbacks and a recording obligation (Complexity Tracking), rather than open-ended investigations? [Measurability, R-002, Plan §Complexity Tracking]

## Dependencies & Assumptions

- [x] CHK032 Is the assumption that all exact pins (verified 2026-09-20) install cleanly together revalidated at Setup, and is the "Prisma `latest` tag is the 8.0 RC — never install it" hazard carried forward as an explicit instruction for tasks? [Assumption, R-001]
- [x] CHK033 Is the Anthropic Messages API dependency pinned as a contract — endpoint, auth header, API version date, and the response fields the native-`fetch` call assumes — given no SDK will absorb provider changes? [Dependency, R-012]
- [x] CHK034 Is the Chromium-only Playwright scope recorded as an accepted assumption (browser support appears in no spec requirement), so the narrowing is deliberate rather than accidental? [Assumption, Plan §Testing, Spec §SC-012]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- An item resolved by changing `contracts/openapi.yaml` or `contracts/schema.prisma` is a contract change: it re-enters through the plan per Principle IV before implementation starts
- This checklist is the `review-plan` gate input; it does not approve the gate
